<!DOCTYPE html>
<html>

<head>
    <title> Game Manager </title>
    <link rel="stylesheet" href="/css/bootstrap.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <nav class="navbar navbar-default">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false"
                    aria-controls="navbar">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                <a class="navbar-left" href="#"><img alt="Brand" class="logo" src="/images/logo.png"></a>
            </div>
            <div id="navbar" class="collapse navbar-collapse navbar-right">
                <ul class="nav navbar-nav">
                    <% if(authdata == null) { %>
                    <li class="active"><a href="/users/register">Register</a></li>
                    <li><a href="/users/login">Login</a></li>
                    <% } else { %>
                    <li><a href="/users/logout">Logout</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <% if(authdata == null) { %>
            <div class="row">
                <div class="col-md-12">
                    <% if(page == '/'){ %>
                        <a href="/games"><img class="jumbo" src="/images/jumbo.png"> </a>
                    <% } %>
                </div>
            </div>
        <% } else { %>
        <div class="row">
            <div class="col-md-3">
                <% if(authdata != null) { %>
                    <p class="well"> Hello, <strong> <%= user.username %>! </strong> </p>
                <% } %>
                <ul class="list-group">
                    <li class="list-group-item"><a href="/games">Games</a></li>
                    <li class="list-group-item"><a href="/genres">Genres</a></li>
                </ul>
            </div>
            <div class="col-md-9">
                <% if(success_msg != false){ %>
                    <div class="alert alert-success">
                        <%= success_msg %>
                    </div>
                <% } %>
                <% if(error_msg != false){ %>
                    <div class="alert alert-danger">
                        <%= error_msg %>
                    </div>
                <% } %>
        <% } %>
